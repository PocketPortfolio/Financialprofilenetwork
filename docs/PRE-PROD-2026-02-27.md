# Pre-Production Readiness — 2026-02-27

**Scope:** Course correction (Lead Magnet CTA) + standard pre-deploy checks.  
**Status:** ✅ Lint & TypeScript pass. Build pipeline started; run full `npm run build` to completion before deploying.

---

## 1. Checks Completed

| Check | Command | Result |
|-------|---------|--------|
| **ESLint** | `npm run lint` | ✅ No warnings or errors |
| **TypeScript** | `npx tsc --noEmit` | ✅ No type errors |
| **Pre-build steps** | `build:importer` → `build:sitemaps` → `build:inject-firebase` → `build:book-assets` | ✅ All completed (sitemaps: 77,149 URLs; book assets verified) |
| **Next.js build** | `next build` | ⏳ Started; run locally to completion before deploy |

---

## 2. Before You Deploy

1. **Run full build to completion** (if not already done):
   ```bash
   npm run build
   ```
   Ensure it finishes with no errors (Next.js may take several minutes).

2. **Commit only what you intend** (per CLAUDE.md):
   - No destructive migrations.
   - Lead Magnet changes: `JsonApiLivePreview.tsx`, `HistoricalDividends.tsx`; doc `COURSE-CORRECTION-ALIGNMENT-REPORT.md`.

3. **Post-deploy verification** (see `docs/DEPLOYMENT-VERIFICATION-CHECKLIST.md`):
   - Vercel deployment success.
   - Smoke-test: `/dashboard`, `/s/xvlxx/json-api`, `/s/xvlxx/dividend-history` (Lead Magnet CTA visible).
   - Critical APIs: `/api/health`, `/api/create-checkout-session` (Stripe), etc.

---

## 3. This Release Includes

- **Lead Magnet CTA** on `/s/[ticker]/json-api` and `/s/[ticker]/dividend-history` (no data gate; CTA above content).
- **Priority 1 (429 → Stripe)** already in place; no code change.
- **Docs:** `COURSE-CORRECTION-ALIGNMENT-REPORT.md`, this file.

---

## 4. Environment & References

- **Design system:** `var(--accent-warm)`, `var(--border-subtle)`, `var(--surface-hover)` (see CLAUDE.md).
- **Production checklist:** `docs/PRODUCTION-READINESS-CHECKLIST.md`, `docs/DEPLOYMENT-VERIFICATION-CHECKLIST.md`.

---

*Run `npm run build` to completion, then push and deploy. Verify on production per the deployment checklist.*
