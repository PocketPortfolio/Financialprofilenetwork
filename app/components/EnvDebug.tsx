'use client';

/**
 * Env debug stub — intentionally does not log any env values, lengths, or prefixes
 * (CodeQL js/clear-text-logging #29–31).
 */
export default function EnvDebug() {
  return null;
}
