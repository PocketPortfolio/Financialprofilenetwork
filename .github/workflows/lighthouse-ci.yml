name: Lighthouse CI
on: 
  pull_request:
    branches: [main, develop]
  workflow_dispatch:  # Manual trigger for main branch - prevents queue buildup
  # Removed automatic push trigger - Lighthouse CI runs on PRs, manual trigger for main

# Concurrency control - prevent queue buildup from multiple commits
concurrency:
  group: lighthouse-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lhci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with: 
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_GA_MEASUREMENT_ID: ${{ secrets.NEXT_PUBLIC_GA_MEASUREMENT_ID }}
      
      - name: Start preview server
        run: npm run preview &
        env:
          PORT: 3001
      
      - name: Wait for server
        run: sleep 10
      
      - name: Run Lighthouse CI
        run: npx @lhci/cli@0.13.x autorun --collect.settings.preset=mobile
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
      
      - name: Upload Lighthouse results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lighthouse-results
          path: .lighthouseci/
